# MySQL-only backend settings (aligned with existing project style)
DB_USER=root
DB_PASSWORD=5780ghks!
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=test

# Optional: if provided, this overrides DB_* settings.
# DATABASE_URL=mysql+pymysql://root:password@127.0.0.1:3306/test?charset=utf8mb4
PORT=5002
